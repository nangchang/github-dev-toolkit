{
  "name": "github-dev-toolkit",
  "version": "0.5.0",
  "description": "A Chrome extension to enhance your GitHub workflow as a developer.",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch",
    "test:build": "tsc -p tsconfig.test.json",
    "test": "npm run test:build && node --test tests/*.test.js",
    "clean": "rm -rf dist",
    "pack": "npm run build && cd dist && zip -r ../github-dev-toolkit-v$npm_package_version.zip * && cd .. && echo '📦 Extension packed to github-dev-toolkit-v'$npm_package_version'.zip'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.264",
    "esbuild": "^0.21.5",
    "typescript": "^6.0.3"
  }
}
